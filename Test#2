local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local localPlayer = Players.LocalPlayer
local camera = workspace.CurrentCamera

-- ========== ПЕРЕМЕННЫЕ ==========
local espEnabled = false
local flyEnabled = false
local noclipEnabled = false
local flyConn = nil
local velocity = nil
local walkSpeed = 16
local flySpeed = 70
local hitboxEnabled = false
local hitboxSize = 4
local hitboxParts = {}
local xrayEnabled = false
local aimbotEnabled = false
local espColor = Color3.fromRGB(255, 0, 0)

-- ========== ESP (КОНТУР) ==========
local function updateESP()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= localPlayer then
            local char = player.Character
            if char then
                local hl = char:FindFirstChild("WRANNE_ESP")
                if espEnabled then
                    if not hl then
                        hl = Instance.new("Highlight")
                        hl.Name = "WRANNE_ESP"
                        hl.FillTransparency = 1
                        hl.OutlineColor = espColor
                        hl.Parent = char
                    end
                else
                    if hl then hl:Destroy() end
                end
            end
        end
    end
end

-- ========== HITBOX EXPANDER (ПРОСТАЯ ВЕРСИЯ) ==========
local function updateHitbox()
    -- Удаляем старые хитбоксы
    for _, data in pairs(hitboxParts) do
        if data then
            if data.part then data.part:Destroy() end
            if data.conn then data.conn:Disconnect() end
        end
    end
    hitboxParts = {}
    
    if not hitboxEnabled then return end
    
    for _, otherPlayer in pairs(Players:GetPlayers()) do
        if otherPlayer ~= localPlayer then
            local char = otherPlayer.Character
            if char then
                -- Ищем к чему прикрепиться
                local attach = char:FindFirstChild("HumanoidRootPart") or 
                               char:FindFirstChild("UpperTorso") or 
                               char:FindFirstChild("Torso")
                if attach then
                    -- Создаём сферу
                    local ball = Instance.new("Part")
                    ball.Shape = Enum.PartType.Ball
                    ball.Size = Vector3.new(hitboxSize, hitboxSize, hitboxSize)
                    ball.Transparency = 1
                    ball.CanCollide = false
                    ball.Name = "WRANNE_Hitbox"
                    ball.Parent = char
                    
                    -- Приклеиваем
                    local weld = Instance.new("Weld")
                    weld.Part0 = attach
                    weld.Part1 = ball
                    weld.Parent = ball
                    
                    table.insert(hitboxParts, {part = ball})
                end
            end
        end
    end
end

local function setHitboxSize(size)
    hitboxSize = size
    for _, data in pairs(hitboxParts) do
        if data.part then
            data.part.Size = Vector3.new(size, size, size)
        end
    end
end

-- ========== X-RAY ==========
local function updateXRay()
    for _, part in pairs(workspace:GetDescendants()) do
        if part:IsA("BasePart") and part.Name ~= "WRANNE_Hitbox" then
            if xrayEnabled then
                if not part:GetAttribute("OriginalMat") then
                    part:SetAttribute("OriginalMat", part.Material)
                    part:SetAttribute("OriginalTrans", part.Transparency)
                end
                part.Material = Enum.Material.Neon
                part.Transparency = 0.3
            else
                local mat = part:GetAttribute("OriginalMat")
                local trans = part:GetAttribute("OriginalTrans")
                if mat then
                    part.Material = mat
                    part.Transparency = trans or 0
                end
            end
        end
    end
end

-- ========== ОСТАЛЬНЫЕ ФУНКЦИИ ==========
local function setSpeed(speed)
    walkSpeed = speed
    local char = localPlayer.Character
    if char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = speed end
    end
end

local function toggleFly()
    local char = localPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("UpperTorso")
    if not hrp then return end
    
    if flyConn then flyConn:Disconnect() end
    if velocity then velocity:Destroy() end
    
    if flyEnabled then
        velocity = Instance.new("BodyVelocity")
        velocity.MaxForce = Vector3.new(1e6, 1e6, 1e6)
        velocity.Parent = hrp
        flyConn = RunService.RenderStepped:Connect(function()
            local camCF = camera.CFrame
            local move = Vector3.zero
            if UIS:IsKeyDown(Enum.KeyCode.W) then move += camCF.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.S) then move -= camCF.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.A) then move -= camCF.RightVector end
            if UIS:IsKeyDown(Enum.KeyCode.D) then move += camCF.RightVector end
            if UIS:IsKeyDown(Enum.KeyCode.Space) then move += Vector3.new(0, 1, 0) end
            if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then move -= Vector3.new(0, 1, 0) end
            velocity.Velocity = move.Magnitude > 0 and move.Unit * flySpeed or Vector3.zero
        end)
    end
end

local noclipConn = nil
local function toggleNoclip()
    if noclipConn then noclipConn:Disconnect() end
    if noclipEnabled then
        noclipConn = RunService.Stepped:Connect(function()
            if localPlayer.Character then
                for _, part in ipairs(localPlayer.Character:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end
        end)
    end
end

-- ========== AIMBOT ==========
RunService.RenderStepped:Connect(function()
    if aimbotEnabled and UIS:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
        local closest, dist = nil, 120
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= localPlayer and player.Character then
                local head = player.Character:FindFirstChild("Head")
                local hum = player.Character:FindFirstChildOfClass("Humanoid")
                if head and hum and hum.Health > 0 then
                    local pos, vis = camera:WorldToViewportPoint(head.Position)
                    if vis then
                        local mag = (Vector2.new(pos.X, pos.Y) - UIS:GetMouseLocation()).Magnitude
                        if mag < dist then
                            dist = mag
                            closest = head
                        end
                    end
                end
            end
        end
        if closest then
            camera.CFrame = CFrame.new(camera.CFrame.Position, closest.Position)
        end
    end
end)

-- ========== ОБНОВЛЕНИЯ ==========
localPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    setSpeed(walkSpeed)
    if flyEnabled then toggleFly() end
    if noclipEnabled then toggleNoclip() end
    if espEnabled then updateESP() end
    if hitboxEnabled then updateHitbox() end
    if xrayEnabled then updateXRay() end
end)

Players.PlayerAdded:Connect(function()
    task.wait(0.5)
    if espEnabled then updateESP() end
    if hitboxEnabled then updateHitbox() end
end)

-- ========== GUI ==========
local Window = Rayfield:CreateWindow({
    Name = "WRANNE",
    LoadingTitle = "WRANNE Loading...",
    LoadingSubtitle = "by WranneScripts",
    ConfigurationSaving = { Enabled = true, FolderName = nil, FileName = "WRANNEConfig" },
    Discord = { Enabled = false },
    KeySystem = false
})

local MainTab = Window:CreateTab("Main", 4483362458)
local VisualsTab = Window:CreateTab("Visuals", 4483362458)
local AdminTab = Window:CreateTab("Admin", 4483362458)

-- Main Tab
MainTab:CreateToggle({
    Name = "ESP Toggle",
    CurrentValue = false,
    Callback = function(v) espEnabled = v; updateESP() end
})

MainTab:CreateToggle({
    Name = "Fly Toggle",
    CurrentValue = false,
    Callback = function(v) flyEnabled = v; toggleFly() end
})

MainTab:CreateSlider({
    Name = "Fly Speed", Range = {10, 150}, Increment = 5, CurrentValue = 70,
    Callback = function(v) flySpeed = v end
})

MainTab:CreateSlider({
    Name = "Walk Speed", Range = {16, 200}, Increment = 1, CurrentValue = 16,
    Callback = function(v) setSpeed(v) end
})

MainTab:CreateToggle({
    Name = "Noclip Toggle",
    CurrentValue = false,
    Callback = function(v) noclipEnabled = v; toggleNoclip() end
})

MainTab:CreateToggle({
    Name = "Aimbot (right mouse)",
    CurrentValue = false,
    Callback = function(v) aimbotEnabled = v end
})

MainTab:CreateToggle({
    Name = "Anti AFK",
    CurrentValue = false,
    Callback = function(v)
        if v then
            local vu = game:GetService("VirtualUser")
            _G.AntiAFK = localPlayer.Idled:Connect(function()
                vu:Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
                task.wait(1)
                vu:Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
            end)
        else
            if _G.AntiAFK then _G.AntiAFK:Disconnect() end
        end
    end
})

-- Visuals Tab
VisualsTab:CreateColorPicker({
    Name = "ESP Color",
    Color = espColor,
    Callback = function(c)
        espColor = c
        for _, player in pairs(Players:GetPlayers()) do
            local char = player.Character
            if char then
                local hl = char:FindFirstChild("WRANNE_ESP")
                if hl then hl.OutlineColor = c end
            end
        end
    end
})

VisualsTab:CreateToggle({
    Name = "Hitbox Expander (ON / OFF)",
    CurrentValue = false,
    Callback = function(v) hitboxEnabled = v; updateHitbox() end
})

VisualsTab:CreateInput({
    Name = "Hitbox Size",
    CurrentValue = "4",
    PlaceholderText = "Enter size (1-1000)",
    Callback = function(v)
        local num = tonumber(v)
        if num and num >= 1 and num <= 1000 then
            setHitboxSize(num)
        end
    end
})

VisualsTab:CreateToggle({
    Name = "X-Ray Vision (Wallhack)",
    CurrentValue = false,
    Callback = function(v) xrayEnabled = v; updateXRay() end
})

-- Admin Tab
AdminTab:CreateButton({
    Name = "Copy Admin Telegram",
    Callback = function()
        setclipboard("https://t.me/WranneScripts")
        Rayfield:Notify({Title = "Copied", Content = "Telegram link copied!"})
    end
})

print("WRANNE HUB загружен! Нажми Insert для открытия меню")
